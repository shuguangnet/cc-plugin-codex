# Changelog

## 0.1.0 (2026-05-29)

### Initial Release

- Plugin manifest and hooks configuration
- Claude Code CLI client with print mode and conversation resume
- Core commands: `/cc:setup`, `/cc:task`, `/cc:review`, `/cc:status`, `/cc:result`, `/cc:cancel`
- Adversarial review with structured JSON output
- Job state persistence and tracking
- Session lifecycle hooks
